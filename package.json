{
  "private": true,
  "description": "Run the app from epic-timeline-planner/ (npm install && npm run dev there). Root dependencies were removed to avoid a second Prisma client under SDLC/node_modules.",
  "dependencies": {
    "recharts": "^3.8.1"
  }
}
